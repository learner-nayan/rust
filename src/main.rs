mod mutable;
mod loops;
mod number_guess;
mod basics;
mod ownership;

fn main() {
    // loops::loops();
    // mutable::mutable();
    // number_guess::number_guess();
    // basics::basics();
    ownership::ownership();
}

